import requests 

##Check for MQTT messages
def check_for_interrupt():
    #thermostat.set_temp = 75
    #if interrupt && interrupt = temp request
        #publish temps
    #elif interrupt && interrupt = set request
        #set temp
        #publish temps 
    return None